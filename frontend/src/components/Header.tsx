import * as React from "react";
import Box from "@mui/material/Box";
import Paper from "@mui/material/Paper";
import Stack from "@mui/material/Stack";
import { styled } from "@mui/material/styles";

export const Header = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <Stack spacing={2}>Headers</Stack>
    </Box>
  );
};
